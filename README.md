NetScanner — это мощная системная утилита на базе Python и PyQt6, предназначенная для глубокого анализа локальной сети, управления общими папками и автоматического исправления сетевых конфигураций Windows для обеспечения бесперебойного доступа к ресурсам.

🔍 Глубокое сканирование сети (Deep Scan)

    Socket Scan (Port 445): В отличие от стандартных средств Windows, программа напрямую сканирует диапазон IP-адресов на наличие открытого порта 445 (SMB), что позволяет обнаруживать скрытые сетевые ресурсы.

    Многопоточность: Использует ThreadPoolExecutor (до 50 потоков одновременно) для мгновенной проверки всей подсети (от .1 до .254).

    Авто-инспекция: При обнаружении активного хоста программа автоматически пытается получить список доступных на нем папок с помощью net view.

🛠 Диагностика и исправление «в один клик»

    Сетевой профиль: Автоматическое переключение сети в режим «Частная» (Private) для снятия системных ограничений.

    Управление службами: Проверка и запуск критически важных служб: LanmanServer, FDResPub, SSDPSRV и upnphost.

    Брандмауэр: Автоматическое создание разрешающих правил для протоколов SMB и Discovery (Discovery_UDP, Discovery_TCP).

    No Lockout: Отключение политики блокировки учетных записей при неверном вводе пароля (net accounts /lockoutthreshold:0).

    Classic Access: Настройка классической модели доступа (ForceGuest=0) и разрешения пустых паролей в локальной сети.

📁 Управление общими ресурсами (Shares)

    Создание шары: Быстрое открытие доступа к любой папке для группы «Все» (Everyone) с полными правами.

    Умный фильтр: Скрытие системного мусора из поиска (Users, PRINT$, IPC$, ADMIN$ и т.д.).

    Принудительное закрытие: Удаление общих ресурсов двойным методом (PowerShell + net share), что позволяет удалять папки даже со спецсимволами (например, !Мой).

🌐 Настройка IP и Адаптеров

    Чистый список: Получение названий адаптеров напрямую через PowerShell, что исключает ошибки кодировки и «иероглифы» в интерфейсе.

    Умная подсказка: Автоматическое определение шлюза и расчет рекомендуемого диапазона статических IP-адресов.

    Быстрая смена: Переключение между DHCP и статическим IP в два клика.

🆘 Аварийный сброс (Factory Reset)

    Полная очистка сетевого стека (winsock reset, int ip reset).

    Сброс Брандмауэра и политик безопасности (через заводской шаблон defltbase.inf).

    Закрытие всех пользовательских общих папок.

🛠 Технический стек

    Язык: Python 3.x

    Интерфейс: PyQt6

    Механизм команд: Base64-кодирование PowerShell скриптов (для обхода проблем с кодировкой и спецсимволами).

    Многопоточность: QThread для GUI и concurrent.futures для сканера.

⚠️ Требования

    Программа требует прав администратора для внесения изменений в настройки сети и реестр.

💡 Использование

    Тест: Запустите диагностику, чтобы увидеть текущее состояние сети.

    Поиск: Нажмите для сканирования сети и обнаружения всех ПК с открытым доступом.

    Шара: Используйте для мгновенного открытия доступа к своим папкам.

    ПКМ по таблице: Правой кнопкой мыши можно открыть папку или закрыть к ней доступ (если она на вашем ПК).
